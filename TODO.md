# Mobile Responsiveness + Navigation + Floating WhatsApp Fixes

## Completed
- [ ] Create a consistent mobile navigation + overflow audit plan (approved).

## To do
- [ ] Fix horizontal overflow root cause(s) across all pages (no horizontal scrolling at 320/375/390/414).
- [ ] Make header consistently accessible while scrolling (mobile).
- [ ] Ensure hamburger is immediately visible inside viewport (far right) on mobile.
- [ ] Fix mobile drawer open behavior (z-index, width, clipping).
- [ ] Standardize floating WhatsApp on `index.html` (icon path + reuse existing Hire Me URL; keep `position: fixed`).
- [ ] Mirror the same navigation + overflow fixes on:
  - [ ] `index.html`
  - [ ] `work.html`
