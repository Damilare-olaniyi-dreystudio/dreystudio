# TODO - Mobile nav + WhatsApp fixes

- [x] Step 1: Fix mobile horizontal overflow on index.html by constraining the tools marquee/transform behavior on small screens so hamburger is immediately visible.

- [x] Step 2: Fix mobile nav panel positioning conflicts by making `#mobile-nav` use a single `fixed` positioning model (remove/override `absolute` behavior) and ensure it stays within viewport with proper z-index.

- [x] Step 3: Ensure header remains accessible while scrolling (sticky/fixed behavior consistency, z-index).

- [x] Step 4: Ensure floating WhatsApp button on index.html uses `assets/icons/whatsapp.svg` icon and remains fixed with required bottom/right spacing.

- [x] Step 5: Update mobile menu open/close code if needed to prevent clipping issues.
- [x] Step 6: Verify on widths 320/375/390/414.


