# TODO - Navigation & Footer Responsiveness Audit

- [ ] Replace navigation markup + mobile menu panel + hamburger JS on `work.html` to match `index.html` exactly.
- [ ] Replace navigation markup + mobile menu panel + hamburger JS on `service.html` to match `index.html` exactly.
- [ ] Replace navigation markup + mobile menu panel + hamburger JS on `about.html` to match `index.html` exactly.
- [ ] Replace navigation markup + mobile menu panel + hamburger JS on `contact.html` to match `index.html` exactly.
- [ ] Replace navigation markup + mobile menu panel + hamburger JS on `book-consultation.html` to match `index.html` exactly.

- [ ] Replace footer markup on `work.html` to match `index.html` footer structure + responsive stacking.
- [ ] Replace footer markup on `service.html` to match `index.html` footer structure + responsive stacking.
- [ ] Replace footer markup on `about.html` to match `index.html` footer structure + responsive stacking.
- [ ] Replace footer markup on `contact.html` to match `index.html` footer structure + responsive stacking (special attention).
- [ ] Replace footer markup on `book-consultation.html` to match `index.html` footer structure + responsive stacking.

- [ ] Run an overflow audit (nav + footer only) for horizontal scroll issues.
- [ ] Manually verify responsiveness at 320 / 375 / 390 / 414 / 768.

